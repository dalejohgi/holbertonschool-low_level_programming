#include "holberton.h"
#include <stdio.h>
/**
 *main - Fucntion to print the name of itself
 *@argc: Counter of the elements unused
 *@argv: String of elements
 *Return: Zero for succes
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);

return (0);
}
